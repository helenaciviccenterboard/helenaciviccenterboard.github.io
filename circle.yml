machine:
  timezone: America/Denver
  ruby:
    version: 2.1.1

test:
  override:
   - bundle exec rake test

deployment:
  staging:
    branch: master
    commands:
      - tar cvzf $CIRCLE_ARTIFACTS/site.tar.gz _site
